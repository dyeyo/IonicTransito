export interface Persons {
    constructor(
        id?: string,
        name?: string,
        last_name?: Date,
        document?: string,
        plate_car?: string,
        model_car?: string,
    )
}
